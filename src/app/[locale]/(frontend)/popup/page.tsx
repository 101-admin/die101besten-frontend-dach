"use client";
import Popup from "@/components/Partners/Popup";
import React from "react";

const popup = () => {
  return (
    <div>
      <Popup open={true} onOpenChange={() => {}} />
    </div>
  );
};

export default popup;
